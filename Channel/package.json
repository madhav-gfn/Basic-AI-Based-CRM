{
  "name": "channel-service",
  "version": "1.0.0",
  "description": "Channel service — dispatches campaign/journey messages via a pluggable provider per channel (defaults to a simulator; real Resend/Twilio adapters are scaffolded in src/providers).",
  "main": "dist/index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.21.2"
  },
  "overrides": {
    "debug": "^4.3.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
